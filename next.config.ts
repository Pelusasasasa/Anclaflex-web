import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  images: {
    domains: ['diubufxllqodxdmorxij.supabase.co']
  }
};

export default nextConfig;
